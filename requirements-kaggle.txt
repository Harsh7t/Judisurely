# Kaggle install — AFTER cloning repo
# Gemma 4 needs transformers from git (stable pip may not have `gemma4` arch yet)
# Install order in notebook Cell 2:
#   1) pip install git+transformers
#   2) pip install -r requirements-kaggle.txt
#   3) pip install gradio --no-deps + deps

accelerate>=0.34.0
bitsandbytes>=0.43.0
torch>=2.1.0
scikit-learn>=1.3.0
fpdf2>=2.7.0
Pillow>=10.0.0
pymupdf>=1.24.0
pydantic>=2.0,<2.11

# transformers installed separately from git in notebook (see Cell 2)
# Gradio installed separately with --no-deps in notebook
